#!/usr/bin/env bash
<<<<<<< HEAD
# Configuring HAProxy load balancer

# update the package list 
sudo apt-get -y update

# install required packages
sudo apt-get -y install software-properties-common

# Add the HAProxy repository and install HAProxy
sudo add-apt-repository -y ppa:vbernat/haproxy-2.0
sudo apt-get -y update
sudo apt-get -y install haproxy

# HAProxy configuration
balancer="\
frontend haproxy_balancer
    bind *:80
    mode http
    default_backend loadservers

backend loadservers
    balance roundrobin
    server 149312-web-01 54.87.215.0:80 check
    server 149312-web-02 100.25.16.32:80 check
"
# Add your HAProxy configuration to haproxy.cfg
sudo cp -a /etc/haproxy/haproxy.cfg{,.orig}
sudo echo "$balancer" >> /etc/haproxy/haproxy.cfg

# Start the HAProxy service
sudo service haproxy start
=======
# install haproxy
SETUP="frontend myapp
        bind *:80
        default_backend myapp_servers
backend myapp_servers
        balance roundrobin
        server server1 54.87.215.0
        server server2 100.25.16.32
"
sudo apt-get -y update
sudo apt-get -y install haproxy
sudo sh -c 'echo "$SETUP" >> /etc/haproxy/haproxy.cfg'
sudo service haproxy restart
>>>>>>> 708fbb0bc9487694265fb44595bf42add32222f9
